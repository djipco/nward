{
  "name": "nward",
  "version": "0.0.3",
  "description": "A library to simplify serial communication between Arduino-compatible microcontrollers and NW.js.",
  "module": "dist/nward.esm.min.js",
  "author": "Jean-Philippe Côté <jp@djip.co>",
  "license": "GPLv3",
  "scripts": {
    "build": "rollup --config",
    "lint": "eslint src",
    "upload": "npm publish",
    "preversion": "npm run lint",
    "version": "npm run build && git add -A dist",
    "postversion": "git push && git push --tags",
    "release:patch": "npm version patch && npm run upload"
  },
  "keywords": [
    "arduino",
    "nw.js",
    "nwjs"
  ],
  "dependencies": {
    "djipevents": "^1.0.0-a",
    "is-array-buffer": "^1.0.1",
    "string-to-arraybuffer": "^1.0.2"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.0",
    "npm-version": "^1.1.0",
    "rollup-plugin-terser": "^5.2.0"
  }
}
